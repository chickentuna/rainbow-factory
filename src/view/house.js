import { CUBE_SIZE } from '../constants.js'
import { backFirst } from '../utils.js'

export function drawHouse ({ cube, rotation = 0 }) {
  // basis is entrance facing SE, no rotation

  return origin
}
